apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keycloak
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: keycloak
    targetRevision: "25.2.0"
    helm:
      values: |
        global:
          imageRegistry: "public.ecr.aws"
          security:
            allowInsecureImages: true
        extraEnvVars:
          - name: KC_DB_URL_HOST
            value: "keycloak-postgresql.infrastructure.svc.cluster.local"
        auth:
          adminUser: admin
          existingSecret: "keycloak-creds"
        production: false 
        proxy: edge
        postgresql:
          enabled: true
          auth:
            username: "keycloak"
            database: "keycloak_db"
            existingSecret: "keycloak-creds"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: infrastructure
  syncPolicy:
    automated:
      prune: true
      selfHeal: true